


<div class="author"><br><br>
  <h1 class="author__heading">Hi, I am Clinton.</h1>
  <p class="author__body">Backend Engineer | Full Stack Developer.</p>
</div>

<img src="/img/" alt="Clinton" class="hero-image" width="981" height="528" />

<section class="articles">
  <h2 class="articles__heading">Latest Posts</h2>

  <ul class="article-ul">
    <% data.forEach(blog=> { %>
    <li>
      <a href="/blog/<%= blog._id %>">
        <span><%= blog.title %></span>
        <span class="article-list__date">
          <%= blog.createdAt.toDateString() %></span>
      </a>
    </li>
    <% }) %>
  </ul>
<% if (nextPage) { %>
  <a href="/?page=<%= nextPage %>" class="pagination">&gt; View Older Posts</a>
<% } %>
</section>
